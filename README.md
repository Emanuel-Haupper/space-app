# Space Explorer

A small React app built with `my-components-library` to showcase how Git submodules work in a real project setup.

This app uses the shared component library as a submodule and demonstrates how reusable UI can be developed once and consumed across apps.